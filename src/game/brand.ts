// Shared visual tokens that must remain independent of application state.
// Keeping the accent outside weddingStore prevents static configuration from
// introducing circular module initialization between store-backed engines.
export const BRAND_ACCENT = '#e2b448';
